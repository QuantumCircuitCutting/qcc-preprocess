[project]
name = "qcc-preprocess"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []  # TODO

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
[tool.setuptools.packages.find]
include = ["circuit_preprocess*"]
